name: GITHUB_OUTPUT
on: push
jobs:
  share:
    runs-on: ubuntu-latest
    steps:
    - id: source
      run: echo "message=Hello" >> "${GITHUB_OUTPUT}"
    - env: 
        result: ${{ steps.source.outputs.message }}
      run: echo "${{ result }}"