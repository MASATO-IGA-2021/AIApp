name: Hello # ワークフロー名
on: push # イベントトリガー
jobs: # ジョブ定義
  hello: # ジョブID
    runs-on: ubuntu-latest # ランナー(実行環境)
    steps: # ステップ定義
      - run: echo "Hello, World!" # コマンド実行
      - uses: actions/checkout@v4 # アクションの呼び出し